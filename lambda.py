 


# This are called as anonymous function. function with no fum=nction name.
# This are also called as lambda function.
# We can pass any number of values but the expressions should be one.


a = lambda x, y : x*y
print(a(4,5))